import {ClickOutsideDirective} from './click-outside.directive';

describe('ClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive: any = ClickOutsideDirective;
    expect(directive).toBeTruthy();
  });
});
